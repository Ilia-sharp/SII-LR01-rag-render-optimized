# ============================================================
# Dev-конфигурация: тестирование + heavy-бэкенд для локальной разработки
# ============================================================
# Используется для запуска pytest и для переключения на полный стек
# sentence-transformers + chromadb локально (RAG_BACKEND=heavy).
#
# На Render Free Tier эта зависимость НЕ ставится — она бы не уместилась в 512 МБ.
# ============================================================

-r requirements.txt

# Heavy-бэкенд (для локальной разработки с ChromaDB)
sentence-transformers>=3.0.0
chromadb>=0.5.5
pymupdf>=1.24.0
pypdf>=4.2.0

# Тестирование
pytest>=8.0.0
httpx>=0.27.0
