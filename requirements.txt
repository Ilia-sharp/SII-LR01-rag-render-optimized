# ============================================================
# Требования для Render Free Tier (512 МБ ОЗУ, 0.1 CPU)
# ============================================================
# Это LIGHT-конфигурация: ONNX Runtime + numpy вместо torch + chromadb.
# RAM во время работы: ~250 МБ (вместо ~1.5 ГБ у heavy-варианта).
#
# Чтобы запустить LOCALLY с тяжёлым стеком (sentence-transformers + chromadb)
# используйте requirements-dev.txt.
# ============================================================

# Ядро сервиса
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# Энкодер: ONNX Runtime вместо torch (~150 МБ вместо ~700 МБ)
onnxruntime>=1.17.0,<2.0
tokenizers>=0.15.0
numpy>=1.26,<2.0
